# AGENTS.md — dc-designer-core v1 行为规范

本项目是面向中国 K12 信息科技/信息技术教师的 Dick–Carey 教学系统设计 Codex 插件。

## 1. 固定范围

- 支持：中国大陆小学、初中、普通高中；学科为信息科技/信息技术。
- 支持模式：`standard_fast`（课标约束快速设计）和 `collaborative`（完整协同设计）。
- 不支持：高校、职教、企业培训和其他学科。遇到这些请求必须返回 `unsupported_scope`，不能套用通用模板。
- 方法：Dick–Carey 教学系统化设计模型。最终项目必须包含需求/目的、教学分析、学习者与环境、绩效目标、评价、策略、材料、形成性评价方案、修订记录和质量门禁。

## 2. 来源和隐私

1. 内置库只提供官方文件元数据、公开链接和 `normalized_summary` 条款候选，不复制商业教材或受限全文。
2. 教师资料仅写入本机 `.dc-designer/knowledge/`，标记为 `C1/teacher_private`，不能伪装成 A1 官方依据。
3. 教学目的正式依据必须经过教师确认；`clause_candidate` 只能称作候选证据。
4. 不保存或输出学生姓名、学号、身份证号、电话、个人成绩等身份信息；只允许匿名聚合的班级共性信息。
5. 形成性评价没有真实数据时，必须标为“待实施/待验证”，不得编造效果数据。

## 3. 两种工作模式

### 3.1 课标约束快速设计

先确认学段、年级、课题、教材版本/单元、课时、设备、班级共性学情和教师已有资料；检索对应课程方案与课程标准，形成可追溯候选；自动生成草案，但教学目的、入门技能、学情、课时设备和教学策略仍必须进入教师确认清单。

### 3.2 完整协同设计

从评价需求和绩效差距开始。每一阶段遵循“提问 → 候选与理由 → 教师确认/修改 → 保存决策 → 下一阶段”，不能用默认比例、成绩或学校要求填空。

## 4. 教学分析图合同

每个项目必须分别生成：

1. 目的操作流程图：教学目的和主要操作步骤的顺序关系；
2. 从属技能与入门技能图：主要步骤、从属技能、入门技能边界和前置关系；
3. 分支/循环/调试课题的程序控制流程图：开始、动作、决策菱形、是/否分支、反馈回路和结束。

两类教学分析图不能合并成一张含义混杂的图。图必须有稳定 ID、无断链/孤立节点/前置循环，文字完整可读；Word 嵌入图片，同时导出可在 diagrams.net 编辑的单页和多页 Draw.io XML。

## 5. 质量门禁

- 教学目的：行为可观察，禁止仅使用“知道、理解、了解”。
- 绩效目标：每项目标有 `CN/B/CR`，且可关联技能节点、评价证据和材料。
- 信息科技编程课题：评价必须包含建模/实现、运行、测试、调试的真实证据，不能只用选择题。
- 来源：官方、教师私有、教材和 AI 推断分开标注。
- 修改：先影响分析，再只应用教师明确采纳的修改，最后重新检查图、目标、评价、策略、材料和来源一致性。
- 只有关键确认、证据门禁、图逻辑和一致性门禁全部通过，才能标记 `final_ready`。

## 6. 公开入口

- `skills/dc-info-tech-design/SKILL.md`
- `skills/dc-info-tech-review/SKILL.md`
- `skills/dc-info-tech-revise/SKILL.md`

本地核心入口为 `scripts/dc_info_tech.py`。`.mcp.json` 和旧 `agent_session` 仅作兼容层，不改变 v1 产品范围。
