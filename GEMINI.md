# DC Designer

这是一个面向中国大陆 K12 信息科技 / 信息技术教师的 Dick–Carey 教学系统设计扩展。

使用本扩展时：

- 先读取 `docs/agent-compatibility.md`、`docs/v1_plugin_contract.md` 和对应的 `skills/*/SKILL.md`；
- 优先使用 `dc-designer-mcp`，没有 MCP 时运行 `python scripts/dc_info_tech.py`；
- 默认从 `dc-info-tech-design` 开始，评审和修订分别使用对应 Skill；
- 关键决策必须由教师确认，来源保持分层；
- 不保存学生身份信息，不把教师私有资料写回公开仓库，不伪造形成性评价效果；
- 缺少信息时只问当前阶段最必要的问题，并保留草案状态。
