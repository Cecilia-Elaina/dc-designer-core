# v1.1.1 发布说明

这是面向发布环境的修复版本，保留 v1.1.0 的产品范围和功能。

## 修复内容

- 修复 Windows 控制台输出中文诊断报告时的编码错误。
- 让发布审计和打包脚本从 manifest 自动读取版本号。
- 将依赖本机 `exports/` 的旧测试改为使用仓库内固定测试夹具。
- 为 CI 的真实 Word 视觉门禁安装 LibreOffice、Poppler 和中文字体。
- 保留 GitHub 源码仓库、安装包、校验文件和本地安装说明。

## 验证

- 针对性测试：61 passed。
- 本地发布审计：pass。
- 本地干净安装验收：pass。
- 形成性评价仍保持“待实施/待验证”，不生成虚构学生效果数据。
